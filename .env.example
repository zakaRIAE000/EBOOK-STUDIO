# Clé API Higgsfield — nécessaire uniquement si on appelle l'API directement
# en dehors du serveur MCP. Le flux normal passe par MCP (voir .mcp.example.json),
# auquel cas cette clé n'est pas requise ici.
HIGGSFIELD_API_KEY=
