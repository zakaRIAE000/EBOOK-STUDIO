# Dependencies / build output
node_modules/
dist/

# Secrets / local machine config — never commit
.env
.mcp.json

# OS / editor noise
.DS_Store
Thumbs.db
npm-debug.log*

# Per-ebook workspace: never tracked. The repo holds the factory, not the books.
workspace/projects/
!workspace/projects/.gitkeep